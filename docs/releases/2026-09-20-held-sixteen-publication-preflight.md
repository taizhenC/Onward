# Sixteen reviewed stories — publication preflight held

Recorded 21 September 2026, 01:37 UTC (20 September local date).

## Owner review and publication request

The owner said: “good, I review it now lets publish that into the production.”
This records editorial review and publication intent for PR #141 at
`b6194397a37f556fd15eb6547851ccb1638a901b`, including the changed matching scopes,
new Rogers text, short-vignette format and disclosed chronology qualifications.
The exact sixteen candidate/proposal pairs are pinned in
[review-targets.json](held-sixteen-integration-2026-09-20/review-targets.json),
SHA-256 `b43c53652f8cf90b4dcf71f94cb84929af38275dd735bf488848bbba8fa714d0`.
The frozen files and their empty draft reviews remain unchanged. This receipt
does not claim a database review transition, approval of an unwritten benchmark,
passing matching evidence, or production publication.

## Fresh deterministic preflight

Root and an independent agent evaluated the proposed fifty-row overlay using
the actual `selectRerankPool` function, existing keyword routes, top-K six, the
real ten-year age-distance rule, and the unchanged 104-case corpus. No provider,
environment file, database, reader/session data or production route was used.

- The current source library has zero nonsemantic prefilter failures.
- The full sixteen-replacement overlay has **fourteen**: zero-based indices
  `8, 12, 16, 19, 36, 43, 48, 54, 58, 60, 67, 78, 79, 90`.
- The expected figures are Rogers, Rachmaninoff, McClintock, Poitier, Edmonia
  Lewis, Lindgren, Carver and Lamarr. This extends the prior packet's narrower
  hard-age-only check; it does not contradict its four disclosed age conflicts.
- Five of the sixteen supplemental proposed positive examples also lose their
  target before reranking: **Lewis, Lindgren, Poitier, Rachmaninoff, Rudolph**.
  The other eleven merely survive retrieval; final matches remain unmeasured.
- Old Rogers cases `8, 12, 58, 100` remain outside the adult episode's age gate.
  The last is semantic and skipped by keyword preflight. None has an accepted
  alternate. Even four errors alone cap non-miss accuracy at 97/101 (96.04%),
  below the unchanged 97.1% release threshold.

These are deterministic retrieval/coverage findings, **not** a real-provider
evaluation or claimed trust-gate run. Existing CI passed at the reviewed head,
but that documentation-only PR does not install or evaluate the new matcher.

## Required next scope

Publication needs both retrieval work and an explicitly reviewed, separately
versioned coverage baseline. Keep the old corpus, thresholds, recipe manifests
and historical evidence unchanged. Carry forward unaffected coverage, document
retired premises and lost coverage, add supported replacements, misses and hard
comparisons, and freeze labels before measuring. The sixteen close-fit examples
alone are not an adequate replacement benchmark.

Code inspection established a content-only path: `datasetForEval` accepts a
registered `EVAL_DATASET_VERSION`; the evaluator verifies its exact file hash,
and library-release governance verifies fresh real-provider passing evidence
for the unchanged promoted recipe at the exact content commit. Consequently,
a new recipe/selector promotion is **not inherently required**. Changing only
`EVAL_DATASET_PATH` is insufficient. No override was used in this preflight.

Separately, fresh GitHub reads show the repository is now public, but main has
no branch protection/effective rules and the recipe-promotion environment is
absent. The older private-plan-403 explanation is stale. These missing controls
still block a genuine recipe promotion; they do not make one necessary for this
content-only route. No repository settings were changed.

The normal story publisher cannot install matching stages. After matching
release evidence passes, use exact bounded stage installation, editorial backup,
receipt-bound review/publication, preservation of all previous publications and
old session foreign keys, followed by worker refresh and ordinary reader checks.
Do not broadly seed all fifty rows or publish new prose against obsolete matching
premises. No merge, database mutation, publication or production deployment was
performed during this preflight. All sixteen remain held pending this work.
